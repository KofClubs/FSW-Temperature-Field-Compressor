版本0.1.0，2020-02-10
0. 编译和运行：
	$ make all
	$ cd bin
	$ ./ftfc -h # 获得帮助
	$ ./ftfc -i ../data/input/T-3mm-2024.dat ../data/output # 举例
	$ make clean # 清除编译产生的文件，包括可执行文件ftfc。
1. 解压器初始化。读入第1帧数据，写出体积元坐标、零时刻温度场。
2. *nix终端执行ftfc [-选项] [参数]，支持短参数：-h、-v、-i、-c、-d。
